# Where to POST when a grade is posted (the advisor's webhook receiver).
ADVISOR_WEBHOOK_URL=http://localhost:5000/api/webhooks/sis/grade-posted

# Leave blank for local demo; set to the same value as advisor-backend's
# SIS_WEBHOOK_TOKEN to require the header on the webhook call.
SIS_WEBHOOK_TOKEN=

PORT=5050
